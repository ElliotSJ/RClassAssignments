---
title: "Foundations in R Assignment pt 1"
author: "Elliot Sprain"
date: "2026-09-24"
output: html_document
---

For your first R Assignment, complete the questions below. Remember, weekly assignments are graded based on attempts and accuracy, so it's ok if you don't get everything immediately. Please submit your answers by 1) updating this file in your GitHub repository and 2) uploading a .html file of the assignment to this repository. Please submit a link to the repository on Canvas. 

# Variables, Vectors, Lists, and Data frames

1. Create a vector called score that starts at 1 and contains every 9th number between 1 and 100. 
```{r}
score <- seq(1,100, 9)
score
```

2. Create a list called participants that contains the participants 1 - 12. 

```{r}
participants <- c(1:12) #just asking for a list of 1-12? 
participants
```

3. Create a list called condition that alternates the words control and experimental six times, for a total of 12 words.

```{r}
condition <- rep(c("control", "experimental"), 6)
condition
```


4. Create a dataframe from the variables in #1-3. 
```{r}
scoresparticipantscondition <- cbind.data.frame(score, participants, condition)
scoresparticipantscondition
```


5. Take the mean of the score column from your dataframe.
```{r}
mean(scoresparticipantscondition$score)
```

# Using data frames 

6. Open the penguins dataframe. Print the dataframe.  
```{r}
library(palmerpenguins)
print(penguins)
```

7. Create a subset of data for Adelie penguins. 
```{r}
library(dplyr)
adeliepenguins <- penguins %>% filter (species=="Adelie")
adeliepenguins
```

8. Summarize this subset of data. 
```{r}
summary(adeliepenguins)
```

8. Create a dataframe that contains penguins with a bill length greater than or equal to the average bill length. 
```{r}
meanbill <- mean(penguins$bill_length_mm, na.rm=TRUE)
meanbill
supermeanbill <- penguins %>% filter(bill_length_mm>=meanbill)
supermeanbill
```

9. How many penguins are in this subset? 

```{r}
nrow(supermeanbill)
```


# More dataset use
10. Load the tidyverse library if you haven't already done so.
```{r}
library(tidyverse)
```

12. Create a new dataframe that has diamonds greater than 1 carat and an ideal cut. 
```{r}
diamonds
caratandcut <- diamonds %>% filter(carat>1 & cut=="Ideal")
caratandcut
```

13. Find the mean and standard deviation of price for this new dataset. 
```{r}
mean(caratandcut$price)
sd(caratandcut$price)
```

14. Create another new dataframe from the original diamonds dataset that includes all diamonds with a price between $2500 and $5000. 

```{r}
midprice <- diamonds %>% filter(price>2500 & price<5000)
midprice
```

15. Which color occurs most in this new subset? 
```{r}
summary(midprice$color)
#F is most common
```

